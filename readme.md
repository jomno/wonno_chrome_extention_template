```bash
  # create a new project using the template
  $ git clone https://github.com/jomno/wonno_chrome_extention_template.git
  # install dependencies and go!
  $ cd wonno_chrome_extention_template
  $ npm install # or yarn
  $ npm run dev # or yarn dev
```
